<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackages" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".LastWaveApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LastWave.Splash"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.LastWave.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">

            <!-- Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Last.fm's web-auth flow redirects here after approval
                 (see AuthRepository.authUrl's cb= param). Opened via Chrome
                 Custom Tabs (real Chrome, not this app's own WebView), so
                 this is what brings the app back to the foreground
                 automatically the moment the person approves — singleTop
                 above means it reuses this same MainActivity instance
                 rather than creating a new one, so Login's in-progress
                 AwaitingApproval state survives the round trip and its
                 onResume check (see LoginScreen.kt) picks up right away. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lastwave" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <!-- LastWave's own local scrobbler (see service/MediaScrobbleListenerService.kt).
             Requires the user to grant Notification Listener access from
             Settings' Scrobbler section — the OS won't let this run without
             it, and the app can't grant it to itself, only send the user
             to the system screen that does. -->
        <service
            android:name=".service.MediaScrobbleListenerService"
            android:exported="true"
            android:label="LastWave Scrobbler"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Owns the native player so audio and its MediaSession survive
             Activity/background transitions. Media3 publishes the standard
             Android now-playing notification and lock-screen controls. -->
        <service
            android:name=".playback.MusicPlaybackService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Four home-screen players. All share the same cross-app active
             MediaSession state, controls and live LastWave color scheme. -->
        <receiver
            android:name=".widget.NowPlayingWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_classic_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/now_playing_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.CompactNowPlayingWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_compact_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/compact_now_playing_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.ArtworkNowPlayingWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_artwork_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/artwork_now_playing_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.GlassNowPlayingWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_glass_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/glass_now_playing_widget_info" />
        </receiver>

        <!-- Retained for the Playlist module's M3U export (SAF/legacy write) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
